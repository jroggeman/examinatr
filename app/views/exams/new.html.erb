<h1>New Exam</h1>
<%= form_for [current_user, @exam] do |f| %>
  <%= f.label :name %>
  <%= f.text_field :name, class: 'form-control' %>

  <br>

  <%= f.submit "Create Exam", class: "btn btn-primary" %><%= link_to "Cancel", user_exams_path(current_user), class: 'btn btn-danger' %>
<% end %>
